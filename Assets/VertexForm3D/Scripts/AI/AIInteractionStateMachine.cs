using System;using UnityEngine;namespace VertexFormCore.AI{    /// <summary>    /// AI交互状态机    /// </summary>    public class AIInteractionStateMachine : MonoBehaviour    {        // 单例实例        public static AIInteractionStateMachine Instance { get; private set; }        // 定义状态枚举        public enum AIState        {            Idle,           // 空闲状态            Listening,      // 监听状态            Processing,     // 处理状态            Executing,      // 执行状态            Feedback        // 反馈状态        }        // 当前状态        public AIState CurrentState { get; private set; } = AIState.Idle;        // 状态变化事件        public event Action<AIState, AIState> OnStateChanged;        private void Awake()        {            if (Instance == null)            {                Instance = this;                DontDestroyOnLoad(gameObject);            }            else            {                Destroy(gameObject);            }        }        /// <summary>        /// 切换到新状态        /// </summary>        /// <param name=